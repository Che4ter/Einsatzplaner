# Build outputs
bin/
einsatzplaner

# Go vendor directory
vendor/

# Generated Wails JS bindings (regenerated via: wails3 generate bindings -b -d frontend/bindings)
frontend/bindings/

# Node.js (only used for local dev tooling — not bundled into the app)
node_modules/
package-lock.json

# macOS
.DS_Store

# IDE / local tooling
.vscode/
.idea/
.claude/

# Sample/test data
sample-*.json
*.xlsx
local_dev